# AGENTS.md

## コミュニケーション

- 原則として日本語で回答する。
- 作業内容、確認結果、未実施の検証がある場合は簡潔に明記する。

## 公開リポジトリとしての配慮

- このリポジトリは公開される前提で扱う。
- 機密情報、個人情報、認証情報、API キー、トークン、Cookie、内部 URL、ローカル環境固有の絶対パスなどをコミットしない。
- ログ、設定ファイル、スクリーンショット、生成物を追加または引用する前に、公開して問題ない内容か確認する。
- サンプル値が必要な場合は、実在の値ではなく `example.com`、`YOUR_API_KEY`、`<placeholder>` などのダミー値を使う。
- 依存関係やビルド成果物を追加する場合は、不要な大容量ファイルや環境依存ファイルが含まれていないか確認する。

## 作業方針

- 既存のコードスタイルと構成に合わせて、変更範囲を必要最小限に保つ。
- 変更後は可能な範囲でテスト、ビルド、型チェック、lint などを実行し、結果を報告する。
- ユーザーが明示的に求めない限り、無関係な変更や大きなリファクタリングは行わない。

## プロジェクト固有のメモ

- パッケージ管理は `yarn.lock` に合わせて Yarn v1 を使う。ローカルに `yarn` がない場合は Corepack 経由で実行する。
- ビルドツールは Vite。通常の確認は `yarn build`、ローカル確認は `yarn preview` を使う。
- Firebase Hosting は `firebase.json` で `build` ディレクトリを公開する設定になっている。Vite の出力先も `build` に合わせている。
- 依存更新後は `yarn audit --json` と `yarn build` を確認する。
