{
  "name": "maze",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^6.4.2"
  },
  "resolutions": {
    "@babel/types": "^7.29.0",
    "debug": "^4.4.3"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "node --test",
    "benchmark:maze": "node scripts/benchmark-maze.mjs",
    "benchmark:trace": "node scripts/benchmark-trace.mjs",
    "preview": "vite preview",
    "deploy": "firebase deploy"
  }
}
